/**
 * Define local vars for the full app
 * (That mean those vars can be access in view with req.locals.<key>
 * @type {{name: string, email: string}}
 */
module.exports = {
    name  : "SpeedFinger",
    email : "contact@chadasaid.net"
};